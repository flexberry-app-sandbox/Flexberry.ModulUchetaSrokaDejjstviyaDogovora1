import { createEnum } from 'ember-flexberry-data/utils/enum-functions';

export default createEnum({
  РешУстнВопр: 'Решение устных вопросов',
  ПомЗакрМес: 'Помощь в закрытии месяца',
  ПомПровДок: 'Помощь в проведении документа'
});
